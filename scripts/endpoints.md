# API Endpoints

Generated by `npx ts-node scripts/list-endpoints.ts`

| Method | Path | Path params | Query | Body | Response | Description |
|--------|------|-------------|-------|-----|----------|-------------|
| GET | `/` | — | — | — | HTML (pug view) | — |
| GET | `/api/blacklist` | — | — | — | { error } | GET /api/blacklist?ids=1,2,3 — per-member blacklists for given member IDs. |
| POST | `/api/blacklist` | — | — | — | { error } | POST /api/blacklist — bulk read (body { ids }) or bulk edit (body { entries: [ { |
| GET | `/api/blacklist/global` | — | — | — | (see handler) | GET /api/blacklist/global — list of globally blacklisted member IDs. |
| POST | `/api/blacklist/global` | — | — | — | (see handler) | POST /api/blacklist/global — bulk add/remove. Body: { add?: number[], remove?: n |
| GET | `/api/hosts` | — | sort (string), all (string), from (string (date or timestamp)), to (string (date or timestamp)), status (string), filled (string) | — | { results } | — |
| GET | `/api/hosts/:id` | id | all (string), host (string), from (string (date or timestamp)), to (string (date or timestamp)), status (string), filled (string) | — | data (see handler for shape) | — |
| GET | `/api/members` | — | ids (string), status (string) | — | { results } | — |
| POST | `/api/members` | — | — | — | (see handler) | — |
| DELETE | `/api/members/:id` | id | — | — | (see handler) | — |
| GET | `/api/members/:id` | id | — | — | (see handler) | — |
| PUT | `/api/members/:id` | id | — | MemberID (number), MemberName (string), DiscordID (number), AllowMerging (number), HostLimit (number), CycleDefault (number), Muted (number), Badge (number), SquadChatTime (number) | (see handler) | — |
| GET | `/api/members/:id/profile` | id | sort (string), all (string), member (string), from (string (date or timestamp)), to (string (date or timestamp)), status (string), filled (string) | — | data (see handler for shape) | — |
| GET | `/api/members/:id/reputation-per-day` | id | sort (string), status (string) | — | { error } | — |
| GET | `/api/members/profiles` | — | sort (string), all (string), from (string (date or timestamp)), to (string (date or timestamp)), status (string), filled (string) | — | { error } | — |
| POST | `/api/members/profiles` | — | sort (string), all (string), from (string (date or timestamp)), to (string (date or timestamp)), status (string), filled (string) | — | { error } | — |
| GET | `/api/primeparts` | — | — | — | { results } | — |
| GET | `/api/primeparts/:setName/:partName` | setName, partName | — | — | part (see handler for shape) | — |
| GET | `/api/primesets` | — | — | — | (see handler) | — |
| GET | `/api/primesets/:setName` | setName | — | — | { error } | — |
| GET | `/api/refinements` | — | — | — | { results } | — |
| GET | `/api/relics` | — | ids (string), status (string) | — | (see handler) | — |
| GET | `/api/relics/:id` | id | — | — | { error } | — |
| GET | `/api/relics/:id/profile` | id | sort (string), all (string), from (string (date or timestamp)), to (string (date or timestamp)), status (string), filled (string) | — | { error } | — |
| GET | `/api/relics/profiles` | — | sort (string), all (string), from (string (date or timestamp)), to (string (date or timestamp)), status (string), filled (string) | — | { error } | — |
| POST | `/api/relics/profiles` | — | sort (string), all (string), from (string (date or timestamp)), to (string (date or timestamp)), status (string), filled (string) | — | { error } | — |
| GET | `/api/squads` | — | isNaN (string), status (string), memberIds (string), relicIds (string), refinementIds (string), era (string), style (string), hostMemberId (string), originatingServerId (string), filled (string) | — | { results } | — |
| GET | `/api/squads/:id` | id | — | — | { error } | — |
| GET | `/api/squads/active` | — | — | — | (see handler) | — |
| GET | `/api/squads/counts-per-day` | — | sort (string), status (string) | — | (see handler) | GET /api/squads/counts-per-day — count of closed squads per day (total, filled,  |
| POST | `/api/squads/create` | — | — | hostMemberId (number), relics (array), relicId (number), offcycle (boolean) | { errors } | Create squads. Body: list of squad creation objects (min 1). Equivalency: relics |
| POST | `/api/squads/join` | — | — | memberId (number), serverId (number), squadIds (array) | { errors } | Join squads (1 or more). Body: { memberId, serverId?, squadIds: string[] }.  Add |
| POST | `/api/squads/leave` | — | — | memberId (number), squadIds (array) | { errors } | Leave squads. Body: { memberId, squadIds?: string[] }.  If squadIds is omitted o |
| GET | `/api/subscriptions` | — | — | — | { error } | GET /api/subscriptions?ids=1,2,3 — bulk read subscriptions (relics, members, hos |
| POST | `/api/subscriptions` | — | — | — | { error } | POST /api/subscriptions with body { "ids": [1,2,3] } — same as GET, for bulk rea |
| POST | `/api/subscriptions/edit` | — | — | — | { error } | POST /api/subscriptions/edit — bulk add/remove. Body: { entries: [ { memberId, r |
| POST | `/auth/login` | — | — | username (string), password (string) | { error } | POST /auth/login — body: { username, password }. Returns { user: { id, username, |
| POST | `/auth/logout` | — | — | — | { ok } | POST /auth/logout — invalidates current session. Requires session (cookie or Bea |
| GET | `/auth/me` | — | — | memberId (number), memberName (string) | { error } | GET /auth/me — returns current user (and linked member if any) if session valid, |
| DELETE | `/auth/me/link` | — | — | — | { error } | DELETE /auth/me/link — remove the member link for the current user. |
| PUT | `/auth/me/link` | — | — | — | { error } | — |
| POST | `/auth/register` | — | — | — | { error } | — |

## Notes

- **Path params** are in the URL path (e.g. `:id`).
- **Query** params are sent as `?key=value`.
- **Body** is for POST/PUT; shape is inferred from Joi schema in the handler.
- **Response** is the general structure of `res.json(...)` (see handler for full shape).